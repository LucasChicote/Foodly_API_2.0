version: '3.8'

services:
  postgres:
    image: 'postgres:15'
    container_name: 'foodly-db'
    environment:
      POSTGRES_DB: 'foodly'
      POSTGRES_USER: 'postgres'
      POSTGRES_PASSWORD: '1234'
    ports:
      - "5432:5432"